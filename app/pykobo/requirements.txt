# flet build（アプリ梱包）専用の依存一覧。
#
# pybunko本体（pyproject.toml）は「ゼロ依存」ポリシーのため
# dependencies = [] のまま——fletのビルドは project.dependencies が
# 空だとこの requirements.txt を読む（flet_cli/build_base.py の優先順:
# poetry deps → project.dependencies → requirements.txt → flet単体）。
# これが無いと flet だけが同梱され、組版プレビュー・印刷・AsciiDoc・
# EPUBが実行時 ModuleNotFoundError で全滅する（実機で発覚）。
#
# GUIが実行時に使うものだけを列挙する（CLI専用のfonttools/pyarrow/
# edge-tts等は含めない）。
flet==0.86.0
pywashi>=0.10.1
pyasciidoc>=0.4.2
ebooklib>=0.18
